<section>
    <h3 class="widget-title">Tags</h3>
    <div class="tags">
        {% for tag in blog.tags %}
            <a href="{{ pcurl('tag/' ~ tag.name|lower) }}"><i class="icon-tag icon-white"></i>{{ tag.name|lower }}</a>
        {% endfor %}
    </div>
</section>